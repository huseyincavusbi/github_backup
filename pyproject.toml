[project]
name = "github-backup"
version = "0.1.0"
description = "GitHub backup and archival suite"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.0",
    "urllib3>=2.2.0",
]
